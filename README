PDFCreator

Creates a PDF/A file from a list of images. This code is used by
Ghent University Library to create PDF files out of scans from
old manuscripts.

Usage:

  cd dist
  ./pdfcreator -v -o test.pdf /tmp/*.tif

Options:

  -v  verbose
  -o  output file
  -p  maximum number of pixels on either axis
